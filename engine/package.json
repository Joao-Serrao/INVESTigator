{
  "name": "@investigator/engine",
  "version": "0.1.0",
  "description": "INVESTigator intelligence engine (TypeScript port) — pure, deterministic logic.",
  "type": "module",
  "private": true,
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "parity": "node --experimental-strip-types test/parity.ts",
    "storage-parity": "node --experimental-strip-types test/storage-parity.ts",
    "ingest-parity": "node --experimental-strip-types test/ingest-parity.ts",
    "pipeline-parity": "node --experimental-strip-types test/pipeline-parity.ts",
    "service-parity": "node --experimental-strip-types test/service-parity.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "js-yaml": "^5.2.2"
  }
}
