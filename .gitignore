# Secrets & local dev data. The shipped apps store everything in
# %APPDATA%\Investraton at runtime; these repo-root files are leftover dev data.
.env
config/portfolio_plan.yaml
config/app_settings.json
config/schedules.json
data/

# deps / local envs (leftover Python venv — retired, not committed)
node_modules/
.venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
