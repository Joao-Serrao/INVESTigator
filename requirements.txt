# Core (v0) — all free
pyyaml>=6.0          # config + plan
requests>=2.31       # http (LLM, discord, generic)
yfinance>=0.2.40     # free prices + .news
feedparser>=6.0      # RSS news feeds
python-dotenv>=1.0   # load .env

# Broker auto-sync (optional — only needed if you enable these sources)
# degiro-connector>=3.0   # UNOFFICIAL Degiro API (fragile, ToS grey area)
# (XTB uses a small built-in WebSocket client — no extra dep)

# LLM brain is swappable and accessed over HTTP:
#   - Ollama: local, runs at http://localhost:11434 (install separately)
#   - Claude: set ANTHROPIC_API_KEY (uses the same `requests` dep)
